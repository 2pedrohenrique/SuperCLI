version: 1
capture_dir: "${TEMP}/supercli/captures"
log_buffer_lines: 10000
notifications:
  enabled: true

workspaces:
  - id: example
    name: My application
    processes:
      - id: backend
        name: Backend
        command: go
        args: ["run", "."]
        dir: "${USERPROFILE}/Documents/Projects/my-app/backend"
        auto_start: false
        ready_patterns:
          - "(?i)(listening|started|ready)"
        error_patterns:
          - "(?i)(build failed|error|panic|exception)"

      - id: frontend
        name: Frontend
        command: npm
        args: ["start"]
        dir: "${USERPROFILE}/Documents/Projects/my-app/frontend"
        auto_start: false
        ready_patterns:
          - "(?i)(compiled successfully|local:|ready)"
        error_patterns:
          - "(?i)(failed to compile|error)"

    actions:
      - id: open-project
        name: Open project in VS Code
        command: code
        args: ["."]
        dir: "${USERPROFILE}/Documents/Projects/my-app"

      - id: project-shell
        name: Open interactive project shell
        command: powershell.exe
        args: ["-NoLogo"]
        dir: "${USERPROFILE}/Documents/Projects/my-app"
        interactive: true

      - id: create-feature
        name: Create feature branch
        command: git
        args: ["checkout", "-b", "feature-{{task}}"]
        dir: "${USERPROFILE}/Documents/Projects/my-app"
        confirm: true
        inputs:
          - id: task
            prompt: Task number or branch suffix
